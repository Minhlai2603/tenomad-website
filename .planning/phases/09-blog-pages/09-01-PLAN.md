# Phase 9: Blog Pages - Plan

## Tasks
1. Generate 4 blog thumbnails using `generate_image` and save to `public/assets/`.
2. Create `src/pages/blog/index.astro` fetching the `blog` collection. Render 2-col grid and the sidebar.
3. Create `src/pages/blog/[...slug].astro` for dynamic routing of blog posts. Implement breadcrumbs, full-width thumbnail, `Content` component rendering, and the sidebar. Add a related posts section.
4. Add basic `.prose` styling in `src/styles/components/blog.css` (or `global.css`) to handle markdown formatting like headings, paragraphs, lists, code blocks, and tables.
