---
phase: 01-project-scaffolding-design-system
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - astro.config.mjs
  - src/styles/variables.css
  - src/styles/global.css
autonomous: true
requirements: [NFR-01, NFR-02, NFR-03, NFR-04]
must_haves:
  truths:
    - "Astro building completes successfully with npm run build"
    - "CSS Custom Properties in variables.css match tokens from DESIGN-SYSTEM.md"
  artifacts:
    - path: package.json
      provides: dependencies and scripts
      contains: astro
    - path: astro.config.mjs
      provides: Astro config settings
      contains: tenomad.com
    - path: src/styles/variables.css
      provides: Design system tokens
      contains: --bg-primary
    - path: src/styles/global.css
      provides: CSS Reset and base styles
      contains: .container
---

<objective>
Initialize the Astro v5 SSG project, configure settings, install dependencies (gsap, swiper), establish the directory structure, and implement the CSS variables/global reset files matching DESIGN-SYSTEM.md and AGENTS.md.

Purpose: Set up the correct development scaffolding for the Tenomad website build.
Output: Initialized Astro project with correct files and folders.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN-SYSTEM.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Astro v5 Project and Dependencies</name>
  <files>package.json</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Run:
    `npm create astro@latest ./ -- --template minimal --no-install --typescript no --y`
    If npm create prompts, use --yes/--y or non-interactive options.
    Then run:
    `npm install`
    Then run:
    `npm install gsap swiper`
    Verify that the dependencies are listed in package.json and no forbidden dependencies (Tailwind, React integration, TypeScript) are present.
  </action>
  <verify>node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs --task 1</verify>
  <done>package.json exists and dependencies are correctly set</done>
</task>

<task type="auto">
  <name>Task 2: Configure Astro settings</name>
  <files>astro.config.mjs</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Modify astro.config.mjs to use:
    site: 'https://tenomad.com'
    output: 'static'
    build.assets: '_assets'
  </action>
  <verify>node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs --task 2</verify>
  <done>astro.config.mjs matches the required settings</done>
</task>

<task type="auto">
  <name>Task 3: Directory Structure Setup</name>
  <files>src/layouts, src/components, src/pages, src/data, src/styles, src/assets, public/assets</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Create folders:
    - src/layouts
    - src/components
    - src/pages
    - src/data
    - src/styles
    - src/assets
    - public/assets
  </action>
  <verify>node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs --task 3</verify>
  <done>All required directories exist</done>
</task>

<task type="auto">
  <name>Task 4: Setup CSS Variables Design Tokens</name>
  <files>src/styles/variables.css</files>
  <read_first>DESIGN-SYSTEM.md, AGENTS.md</read_first>
  <action>
    Create src/styles/variables.css and define design tokens in :root:
    - Backgrounds: --bg-primary (#FFF), --bg-secondary (#F8F9FA), --bg-tertiary (#E9ECEF), --bg-hero (linear-gradient(135deg, #0B0F19 0%, #1E1B4B 100%)), --bg-footer (#0B0F19)
    - Text: --text-primary (#1A1D20), --text-secondary (#495057), --text-muted (#6C757D), --text-on-dark (#FFFFFF), --text-on-hero (#FFFFFF)
    - Brand Colors: --accent-gold (#FFB800), --accent-blue (#0055FF), --accent-indigo (#3730A3)
    - Neon: --neon-cyan (#00F0FF), --neon-blue (#0047FF), --neon-purple (#AD00FF)
    - Typography: Font family 'Inter', system-ui, sans-serif; --text-xs through --text-hero.
    - Spacing: 4px base scale: --space-1 (4px) to --space-30 (120px)
    - Effects: shadows, glassmorphism, border radii.
  </action>
  <verify>node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs --task 4</verify>
  <done>src/styles/variables.css contains all design system custom properties</done>
</task>

<task type="auto">
  <name>Task 5: Setup global.css and CSS reset</name>
  <files>src/styles/global.css</files>
  <read_first>AGENTS.md</read_first>
  <action>
    Create src/styles/global.css and import variables.css.
    Add standard CSS reset rules: box-sizing, margin/padding reset, body background, typography base settings using variables.
    Create helper classes: .container (max-width: 1200px), .section, and .visually-hidden.
  </action>
  <verify>node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs --task 5</verify>
  <done>src/styles/global.css created with css reset and core classes</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node .planning/phases/01-project-scaffolding-design-system/verify-scaffold.cjs
- [ ] npm run build completes with zero errors
</verification>

<success_criteria>
- All tasks completed successfully
- Custom verification script passes
- Astro scaffolding compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-scaffolding-design-system/01-01-SUMMARY.md`
</output>
