# Phase 8: Careers & Contact Pages - Plan

## Tasks
1. Generate `hue-map.webp` using `generate_image` and save it to `public/assets/`.
2. Create `src/components/ContactForm.astro` ensuring all a11y standards (labels, IDs).
3. Create `src/pages/contact.astro` incorporating the form, contact info, and static map.
4. Create `src/pages/careers.astro` with the banner, benefits grid, and an accordion for job positions consuming `src/data/careers.json`.
5. Implement GSAP animations and client-side interactions for the accordion.
