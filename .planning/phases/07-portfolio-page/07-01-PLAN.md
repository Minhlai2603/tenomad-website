# Phase 7: Portfolio Page - Plan

## Tasks
1. Generate the missing 8 portfolio thumbnail images and save them as `.webp` files in `public/assets/`.
   - homing
   - drberg
   - sqoop
   - swolematch
   - leedpop
   - xeke
   - mycalsigns
   - vespasafari
2. Create `PortfolioCard.astro` component in `src/components/`. 
   - Should accept `title`, `tags`, `thumbnail`, `description` props.
   - Use CSS custom properties for styling.
   - Include hover overlay with "View Detail".
3. Create `src/pages/portfolio.astro`.
   - Render a banner section for the page intro.
   - Render a tab filter bar (`All`, `Design`, `Website`, `Mobile`, `Startup`, `E-commerce`, `Hybrid App`).
   - Map over `src/data/portfolio.json` to render the grid of `PortfolioCard`s.
4. Implement filtering logic and animations.
   - Use vanilla JS inside a `<script>` island.
   - Use GSAP for smooth layout transitions when filtering the items.
5. Create CSS module/component styling in `src/styles/components/portfolio.css` and import into the page/components.
