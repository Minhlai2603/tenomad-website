---
phase: 02-layout-header-footer
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/layouts/Layout.astro
  - src/components/Header.astro
  - src/components/Footer.astro
  - src/pages/index.astro
  - src/styles/components/header.css
  - src/styles/components/footer.css
autonomous: true
requirements: [FR-11, FR-12, NFR-05, NFR-06, NFR-07]
must_haves:
  truths:
    - "Astro building completes successfully with npm run build"
    - "The layout includes preconnect Google Fonts links and imports variable and global stylesheets"
    - "Header implements sticky glass effect with Services dropdown and mobile hamburger menu"
    - "Footer includes sitemap navigation, exact contact info, and social links"
  artifacts:
    - path: src/layouts/Layout.astro
      provides: main layout template
      contains: canonical
    - path: src/components/Header.astro
      provides: global site header
      contains: Let's Talk
    - path: src/components/Footer.astro
      provides: global site footer
      contains: contact@tenomad.com
    - path: src/styles/components/header.css
      provides: BEM header styling
      contains: .header
    - path: src/styles/components/footer.css
      provides: BEM footer styling
      contains: .footer
---

<objective>
Build the core Layout, Header, and Footer components. Establish the global HTML structure, SEO metadata, sticky header behavior (desktop hover dropdown, mobile accordion, hamburger overlay), and dark-themed footer.
</objective>

<execution_context>
@.agent/gsd-core/workflows/execute-plan.md
@.agent/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN-SYSTEM.md
@AGENTS.md
@CONTENT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Layout Component</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>AGENTS.md, DESIGN-SYSTEM.md, src/styles/global.css</read_first>
  <action>
    Create src/layouts/Layout.astro.
    - Set up a standard HTML5 skeleton.
    - Declare props: pageTitle (string) and pageDescription (string).
    - Set document title: "{pageTitle} | Tenomad".
    - Add meta description, viewport, and charset.
    - Compute canonical URL dynamically using Astro.url.pathname and https://tenomad.com.
    - Configure Open Graph tags (og:title, og:description, og:type="website", og:url, og:image="/assets/og-image.png").
    - Preconnect to Google Fonts and load the Inter stylesheet (weights 400 to 800).
    - Import src/styles/variables.css and src/styles/global.css.
    - Use `<slot />` inside `<body>` to allow child page wrapping.
  </action>
  <verify>node .planning/phases/02-layout-header-footer/verify-layout.cjs --task 1</verify>
  <done>src/layouts/Layout.astro successfully handles title, description, and canonical tags</done>
</task>

<task type="auto">
  <name>Task 2: Build Header Component and CSS</name>
  <files>src/components/Header.astro, src/styles/components/header.css</files>
  <read_first>AGENTS.md, CONTENT.md, DESIGN-SYSTEM.md</read_first>
  <action>
    Create src/components/Header.astro and src/styles/components/header.css.
    - Header must be sticky, styling with --glass-bg-light, backdrop-filter blur, and glass-border bottom border.
    - Logo on the left, navigation in the center, and "Let's Talk" CTA button on the right (pointing to /contact).
    - Navigation links: Home (/), About Us (/about-us), Services (/services), Portfolio (/portfolio), Careers (/careers), Blog (/blog), Contact (/contact).
    - Services item has a dropdown.
    - Desktop: Hover triggers services dropdown display using pure CSS transitions.
    - Mobile: Hamburger button (aria-expanded, aria-controls, min touch target 48x48px) triggers full-screen mobile menu overlay.
    - Services item on mobile operates as an accordion panel that expands/collapses.
    - Add simple inline `<script>` at the bottom of Header.astro to handle hamburger and accordion toggling.
  </action>
  <verify>node .planning/phases/02-layout-header-footer/verify-layout.cjs --task 2</verify>
  <done>Header and stylesheet created with functioning menus and accessible targets</done>
</task>

<task type="auto">
  <name>Task 3: Build Footer Component and CSS</name>
  <files>src/components/Footer.astro, src/styles/components/footer.css</files>
  <read_first>AGENTS.md, CONTENT.md, DESIGN-SYSTEM.md</read_first>
  <action>
    Create src/components/Footer.astro and src/styles/components/footer.css.
    - Footer uses dark background --bg-footer and text --text-on-dark.
    - 3 columns layout:
      1. Logo & slogan: "You Dream It, We Build It".
      2. Quick links sitemap.
      3. Contact info matching CONTENT.md verbatim:
         - Address: 26 Ly Thuong Kiet Street, Vinh Ninh Ward, Hue City, Vietnam
         - Phone: (+84)02346 27 17 57
         - Email: contact@tenomad.com
    - Bottom bar: Copyright & social icons linking verbatim to Facebook (https://facebook.com/tenomad), LinkedIn (https://linkedin.com/company/tenomad), and Twitter/X (https://x.com/tenomad).
    - Social links transition to active glow state on hover.
  </action>
  <verify>node .planning/phases/02-layout-header-footer/verify-layout.cjs --task 3</verify>
  <done>Footer and stylesheet created with exact contact details and social links</done>
</task>

<task type="auto">
  <name>Task 4: Integrate Header and Footer in Layout</name>
  <files>src/layouts/Layout.astro</files>
  <read_first>src/layouts/Layout.astro, src/components/Header.astro, src/components/Footer.astro</read_first>
  <action>
    Update src/layouts/Layout.astro:
    - Import Header and Footer components in the frontmatter.
    - Render Header above the `<main>` tag wrapping the `<slot />`.
    - Render Footer below the `<main>` tag.
  </action>
  <verify>node .planning/phases/02-layout-header-footer/verify-layout.cjs --task 4</verify>
  <done>Layout renders Header and Footer globally around page content</done>
</task>

<task type="auto">
  <name>Task 5: Refactor index.astro to use Layout</name>
  <files>src/pages/index.astro</files>
  <read_first>src/pages/index.astro, src/layouts/Layout.astro</read_first>
  <action>
    Modify src/pages/index.astro:
    - Wrap the homepage content with the Layout component.
    - Pass pageTitle="Home" and pageDescription="We translate our clients dreams into reality." props.
    - Keep a single `<h1>` tag inside the page.
  </action>
  <verify>node .planning/phases/02-layout-header-footer/verify-layout.cjs --task 5</verify>
  <done>index.astro conforms to Layout wrapping and metadata structure</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] node .planning/phases/02-layout-header-footer/verify-layout.cjs
- [ ] npm run build completes with zero errors
</verification>

<success_criteria>
- All tasks completed successfully
- Custom verification script passes
- Layout, Header, and Footer rendered on homepage
- Astro compilation runs successfully
</success_criteria>

<output>
After completion, create `.planning/phases/02-layout-header-footer/02-01-SUMMARY.md`
</output>

## Artifacts this phase produces
- `src/layouts/Layout.astro` (layout component)
- `src/components/Header.astro` (header component)
- `src/components/Footer.astro` (footer component)
- `src/styles/components/header.css` (header stylesheet)
- `src/styles/components/footer.css` (footer stylesheet)
