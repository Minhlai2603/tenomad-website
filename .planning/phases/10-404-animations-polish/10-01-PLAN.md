# Phase 10: 404, Animations & Polish - Plan

## Tasks
1. Create `src/pages/404.astro`.
2. Create `src/scripts/animations.js` implementing a global animation runner for `data-animate="fade-up"` using `gsap` and `ScrollTrigger`.
3. Include `animations.js` in `src/layouts/Layout.astro`.
4. Ensure global focus styles exist in `src/styles/global.css` (or variables) for accessibility.
